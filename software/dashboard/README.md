# Introduce this web

software/
└── dashboard/
    ├── index.html          # Trang chính dashboard
    ├── css/
    │   └── style.css       # Chứa toàn bộ CSS (body, card, overlay, canvas...)
    ├── js/
    │   ├── main.js         # Logic chart + dữ liệu giả + interval update
    │   └── overlay.js      # Logic phóng to chart khi click overlay
    └── libs/
        └── chart.min.js    # Chart.js (hoặc dùng CDN)